{
  "name": "@zeep/contract",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "compact compile src/zeep.compact managed/zeep",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
